{
  "config": {
    "step": {
      "user": {
        "title": "Set up Helmsman",
        "description": "Configure the automation helper. Set the Ollama fields to enable LLM improvement suggestions, or leave the URL blank to run rules-based audits only.",
        "data": {
          "ollama_url": "Ollama server URL",
          "model": "Ollama model",
          "scan_interval_hours": "Audit interval (hours)",
          "stale_days": "Stale automation threshold (days)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the Ollama server at that URL. Fix the URL or leave it blank to configure later."
    },
    "abort": {
      "already_configured": "Helmsman is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Helmsman options",
        "data": {
          "ollama_url": "Ollama server URL",
          "model": "Ollama model",
          "scan_interval_hours": "Audit interval (hours)",
          "stale_days": "Stale automation threshold (days)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the Ollama server at that URL. Fix the URL or leave it blank to configure later."
    }
  },
  "entity": {
    "sensor": {
      "findings": {
        "name": "Findings"
      },
      "suggestions": {
        "name": "Suggestions"
      }
    }
  },
  "issues": {
    "missing_entity": {
      "title": "Automation references a missing entity",
      "fix_flow": {
        "step": {
          "init": {
            "title": "Fix with Helmsman",
            "description": "{detail}\n\nAutomation: {automation}\n\nOpen the [Helmsman panel](/helmsman) and press **Review flagged** — the local LLM proposes a validated fix you can approve and apply with one click (a snapshot is saved first, with one-click rollback).\n\nSubmitting closes this repair; it returns on the next audit if the problem remains."
          }
        }
      }
    },
    "unavailable_entity": {
      "title": "Automation references an unavailable entity",
      "fix_flow": {
        "step": {
          "init": {
            "title": "Fix with Helmsman",
            "description": "{detail}\n\nAutomation: {automation}\n\nOpen the [Helmsman panel](/helmsman) and press **Review flagged** — the local LLM proposes a validated fix you can approve and apply with one click (a snapshot is saved first, with one-click rollback).\n\nSubmitting closes this repair; it returns on the next audit if the problem remains."
          }
        }
      }
    },
    "deprecated_service_key": {
      "title": "Automation uses deprecated service syntax",
      "fix_flow": {
        "step": {
          "init": {
            "title": "Fix with Helmsman",
            "description": "{detail}\n\nAutomation: {automation}\n\nOpen the [Helmsman panel](/helmsman) and press **Review flagged** — the local LLM proposes a validated fix you can approve and apply with one click (a snapshot is saved first, with one-click rollback).\n\nSubmitting closes this repair; it returns on the next audit if the problem remains."
          }
        }
      }
    },
    "deprecated_trigger_platform": {
      "title": "Automation uses deprecated trigger syntax",
      "fix_flow": {
        "step": {
          "init": {
            "title": "Fix with Helmsman",
            "description": "{detail}\n\nAutomation: {automation}\n\nOpen the [Helmsman panel](/helmsman) and press **Review flagged** — the local LLM proposes a validated fix you can approve and apply with one click (a snapshot is saved first, with one-click rollback).\n\nSubmitting closes this repair; it returns on the next audit if the problem remains."
          }
        }
      }
    }
  },
  "services": {
    "run_audit": {
      "name": "Run audit",
      "description": "Run a Helmsman automation audit now instead of waiting for the next scheduled pass."
    },
    "review_automation": {
      "name": "Review automation",
      "description": "Ask the local LLM to review an automation and propose a validated improvement. Read-only — nothing is applied.",
      "fields": {
        "entity_id": {
          "name": "Automation",
          "description": "The automation to review. Leave empty to review every automation with an error or warning finding."
        }
      }
    },
    "draft_automation": {
      "name": "Draft automation",
      "description": "Draft a new automation from a plain-language description. The draft is held in the Helmsman panel for review — nothing is created until you approve it there.",
      "fields": {
        "description": {
          "name": "Description",
          "description": "What should happen, in plain language."
        }
      }
    }
  }
}
